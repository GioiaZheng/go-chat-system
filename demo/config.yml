# Sample configuration for the API service; see `cmd/webapi/main.go` for wiring defaults.

log:
  level: debug
#  methodname: false      # Include the calling method in log entries.
#  json: false            # Emit JSON lines instead of plain text.
#  destination: stderr    # Redirect logs to a file path when needed.
#  file: /tmp/debug.log   # Example file path for persistent logging.
#  combinedtostdout: true # Mirror log output to stdout for container logs.
#web:
#  apihost: 0.0.0.0:3000   # Address for the API listener.
#  debughost: 0.0.0.0:4000 # Address for the debug/pprof listener.
#  readtimeout: 5s         # Maximum duration for reading the full request.
#  writetimeout: 5s        # Maximum duration before timing out writes.
#  shutdowntimeout: 5s     # Grace period before forceful shutdown.
#  behindproxy: false      # Enable when running behind a reverse proxy.
